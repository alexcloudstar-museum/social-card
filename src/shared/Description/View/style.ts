import { Fragment } from 'react';
import styled from 'styled-components';

export const TextWrapper = styled(Fragment)`
  h5 {
    font-weight: 400;
    font-size: 16px;
  }
  span {
    color: #63afe7;
  }
`;
